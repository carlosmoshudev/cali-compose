services:
  jdownloader:
    image: jlesage/jdownloader-2
    container_name: jdownloader
    restart: unless-stopped
    ports:
      - "5800:5800"
    environment:
      - TZ=${TIMEZONE}
    volumes:
      - ${APPDATA}/jdownloader:/config
      - ${DOWNLOADS}/jdownloader:/output
